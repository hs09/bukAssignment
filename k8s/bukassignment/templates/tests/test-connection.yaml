apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bukassignment.fullname" . }}-test-connection"
  labels:
    {{- include "bukassignment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bukassignment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
